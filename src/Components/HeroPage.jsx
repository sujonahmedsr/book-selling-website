import BarMove from "./BarMove";
import HeroArea from "./HeroArea";

const HeroPage = () => {
    return (
        <div>
            <HeroArea></HeroArea>
            <BarMove></BarMove>
        </div>
    );
};

export default HeroPage;